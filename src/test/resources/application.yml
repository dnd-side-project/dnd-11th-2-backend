spring:
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: false
    defer-datasource-initialization: true

  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:16.3://test

app:
  api:
    allow-origins: "*"
  auth:
    token:
      access:
        expiration: PT1H
        secret-key: test-long-long-long-secret-key

oauth:
  apple:
    public-key-url: test-apple-url
